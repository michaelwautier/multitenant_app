<h1>New review for <%= @product.name %></h1>

<%= form_for([@product, @product_review]) do |f| %>
  <%= f.label :content %>
  <%= f.text_field :content %>
  <%= f.label :rating %>
  <%= f.number_field :rating %>
  <br/>
  <br/>
  <%= f.submit %>
<% end %>
